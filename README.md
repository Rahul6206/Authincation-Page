### Authentication Page
This is a Complete authentication page to handle user login and singup
### 🔧 Installation & Setup
1. Clone the repository:
   ```
   git clone https://github.com/Rahul6206/Authentication.git
   cd Authentication
   ```
2. Install dependencies:
   ```
   npm install
   ```
3. Set up environment variables:
   ```
    PORT=8080
    JWT_SECRET=
    MONGO_URI=
   ```
4. Run the development server:
    ```bash
    cd Backend
    npm run dev
    # Server running on PORT 8080
    cd ../Frontend
    npm run dev
    # Server running on localhost:5173
    ```
   
    
